# Script: postprocessOSData
## Introduction
The script is written in Python, which is used to post-process soot volume fractions and generated results 
from 1DOS++ solver (from CRECK solver) and then write them as .csv file.
